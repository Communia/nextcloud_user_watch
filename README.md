# User Watch

Whenever a user is updated, created or password changed, it will 
dump users table to a specified path (suffixed with `_user_dump.txt`) and will
ping a specified url.

Settings on `/settings/admin`

